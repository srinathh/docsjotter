package utils

/*
commenting out this test to prevent GoConvey opening the test file with every directory change
import (
	"testing"
)

func TestStart(t *testing.T) {
	if err := Start("test.txt"); err != nil {
		t.Fatalf("Error %s in starting test.txt", err)
	}
}
*/
